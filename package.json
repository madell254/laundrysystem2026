{
  "name": "@workspace/api-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "node setup-db.mjs && node --enable-source-maps standalone-entry.mjs",
    "start": "node --enable-source-maps standalone-entry.mjs",
    "build": "echo 'no build step required'",
    "db:migrate": "node setup-db.mjs",
    "postinstall": "echo 'install complete'",
    "typecheck": "echo 'no typecheck'"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.13.1"
  }
}
